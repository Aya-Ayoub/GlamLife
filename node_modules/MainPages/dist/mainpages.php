
<?php
// Database connection
$conn = new mysqli("localhost", "admin", "password", "glamlife");

// Check if the connection was successful
if ($conn->connect_error) {
    die("Connection failed: " . $conn->connect_error); // If the connection fails, terminate the script and display the error
}

// Get the category ID from the query string, or set default to null if not provided
$category_id = isset($_GET['category_id']) ? $_GET['category_id'] : null;
$User_ID = isset($_SESSION['user_id'] ) ? $_SESSION['user_id'] : null;

// Set a default title for the page
$title = "Shop in Style";

// Change the page title based on the category ID provided in the URL
if ($category_id == 1) {
    $title = "Hair Care Products"; // Hair Care Products category
} elseif ($category_id == 3) {
    $title = "Skin Care Products"; // Skin Care Products category
} elseif ($category_id == 2) {
    $title = "Body Care Products"; // Body Care Products category
} elseif ($category_id == 4) {
    $title = "Makeup Products"; // Makeup Products category
}

// Handle search input from the form, if provided
$search_query = isset($_POST['query']) ? trim($_POST['query']) : '';
?>

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />
    <meta name="description" content="" />
    <meta name="author" content="" />
    <title><?php echo $title; ?> - GlamLife</title> <!-- Set the page title dynamically -->
    <!-- Favicon -->
    <link rel="icon" type="image/x-icon" href="assets/favicon.ico" />
    <!-- Bootstrap icons for styling -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.5.0/font/bootstrap-icons.css" rel="stylesheet" />
    <!-- Core theme CSS (includes Bootstrap for responsive layout) -->
    <link href="css/styles.css" rel="stylesheet" />
</head>
<body>
    <!-- Navigation bar with links and search form -->
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container px-4 px-lg-5">
        <!-- Link to home page -->
        <a class="navbar-brand" href="http://localhost/GlamLife/GlamLife/node_modules/MainPages/dist/mainpages.php">
            <img src="assets/images/GlamLifeLogo.png" alt="GlamLife Logo" style="height: 60px; width: auto; margin-right: 10px;" />
        </a>
        <!-- Hamburger menu for mobile navigation -->
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation"><span class="navbar-toggler-icon"></span></button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <!-- Navigation links -->
            <ul class="navbar-nav me-auto mb-2 mb-lg-0 ms-lg-4">
                <li class="nav-item"><a class="nav-link active" aria-current="page" href="http://localhost/GlamLife/GlamLife/node_modules/MainPages/dist/mainpages.php">Home</a></li>
                <li class="nav-item dropdown">
                    <!-- Shop dropdown menu -->
                    <a class="nav-link dropdown-toggle" id="navbarDropdown" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">Shop</a>
                    <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                        <li><a class="dropdown-item" href="http://localhost/GlamLife/GlamLife/node_modules/MainPages/dist/mainpages.php?category_id=1">Hair-Care Products</a></li>
                        <li><a class="dropdown-item" href="http://localhost/GlamLife/GlamLife/node_modules/MainPages/dist/mainpages.php?category_id=2">Body-Care Products</a></li>
                        <li><a class="dropdown-item" href="http://localhost/GlamLife/GlamLife/node_modules/MainPages/dist/mainpages.php?category_id=3">Skin-Care Products</a></li>
                        <li><a class="dropdown-item" href="http://localhost/GlamLife/GlamLife/node_modules/MainPages/dist/mainpages.php?category_id=4">Makeup Products</a></li>
                    </ul>
                </li>
            </ul>
            <!-- Search form -->
            <form class="d-flex mx-auto" method="POST" action="">
                <input class="form-control me-2" type="search" name="query" placeholder="Search products" aria-label="Search" value="<?php echo htmlspecialchars($search_query); ?>">
                <button class="btn btn-outline-pink" type="submit">Search</button>
            </form>
            <!-- Cart button -->
            <form class="d-flex ms-3">
                <a class="btn btn-outline-pink" href="http://localhost/GlamLife/GlamLife/cart.php">
                    <i class="bi-cart-fill me-1"></i>
                    Cart
                    <span class="badge bg-pink text-dark ms-1 rounded-pill">0</span>
                </a>
            </form>
            <!-- Logout button -->
            <a class="btn btn-outline-pink ms-3" href="\GlamLife\GlamLife\loginsignup.php">Logout</a> <!-- Adjust href to your login or logout handler -->
        </div>
    </div>
</nav>

    <?php if ($category_id): ?>
    <!-- Display category-specific title if category_id is set -->
    <header class="bg-pink" style="padding-top: 2px; padding-bottom: 2px;">
        <div class="container px-4 px-lg-5 my-5">
            <div class="text-center text-white">
                <h1 class="display-4 fw-bolder"><?php echo $title; ?></h1>
                <p class="lead fw-normal text-white-50 mb-0">From GlamLife</p>
            </div>
        </div>
    </header>
    <?php else: ?>
    <!-- Display general title if no category is selected -->
    <header class="bg-pink py-5">
        <div class="container px-4 px-lg-5 my-5">
            <div class="text-center text-white">
                <h1 class="display-4 fw-bolder"><?php echo $title; ?></h1>
                <p class="lead fw-normal text-white-50 mb-0">With GlamLife</p>
            </div>
        </div>
    </header>
    <?php endif; ?>

    <!-- Products display section -->
    <section class="py-5">
        <div class="container px-4 px-lg-5 mt-5">
            <div class="row gx-4 gx-lg-5 row-cols-2 row-cols-md-3 row-cols-xl-4 justify-content-center">
                <?php
                    // Query to fetch products based on search query or category ID
                    if (!empty($search_query)) {
                        if ($category_id) {
                            //search and view products the the input names in a specific category by checking whether the search matches any names, and if the C_id matches the category_id
                            $stmt = $conn->prepare("SELECT Product_ID, Product_Name, Product_Image, Product_Price FROM products WHERE Product_Name LIKE ? AND C_ID = ? ORDER BY RAND()");
                            $like_query = "%" . $search_query . "%"; //the % is used for searching for something 'like' the input in the search
                            $stmt->bind_param("si", $like_query, $category_id); //the s means string value, and likequery is the value, i means intiger and categoryid is the integer
                        } else {
                            //search and view products the the input names in any category by checking whether the search matches any names
                            $stmt = $conn->prepare("SELECT Product_ID, Product_Name, Product_Image, Product_Price FROM products WHERE Product_Name LIKE ? ORDER BY RAND()");
                            $like_query = "%" . $search_query . "%"; //the % is used for searching for something 'like' the input in the search
                            $stmt->bind_param("s", $like_query);   //the s means string value, and likequery is the value
                        }
                        $stmt->execute(); //execute the search
                        $result = $stmt->get_result(); //get the results
                    } elseif ($category_id) {
                        //if there is a category but no search then display all products in that category
                        $stmt = $conn->prepare("SELECT Product_ID, Product_Name, Product_Image, Product_Price FROM products WHERE C_ID = ? ORDER BY RAND()");
                        $stmt->bind_param("i", $category_id);   //i means intiger and categoryid is the integer
                        $stmt->execute();
                        $result = $stmt->get_result();
                    } else {
                        //if there is no category and no search then display all products in all categories
                        $result = $conn->query("SELECT Product_ID, Product_Name, Product_Image, Product_Price FROM products ORDER BY RAND()");
                    }

                    //displaying the products in cards each
                    if ($result->num_rows > 0) {
                        while($row = $result->fetch_assoc()) {
                            echo "<div class='col mb-5'>";
                            echo "<div class='card h-100'>";
                            echo "<img class='card-img-top' src='" . $row['Product_Image'] . "' alt='Product Image' />"; //displays the image from the database
                            echo "<div class='card-body p-4'>";
                            echo "<div class='text-center'>";
                            echo "<h5 class='fw-bolder'>" . $row['Product_Name'] . "</h5>"; //display the name from the database
                            echo "<p class='text-muted'>£" . number_format($row['Product_Price'], 2) . "</p>"; //display price from the database
                            echo "</div>";
                            echo "</div>";
                            echo "<div class='card-footer p-4 pt-0 border-top-0 bg-transparent'>";
                            echo "<div class='text-center'><a class='btn btn-outline-pink mt-auto' href='http://localhost/GlamLife/GlamLife/product.php?product_id=" . $row['Product_ID'] . "'>View Product</a></div>";
                            echo "</div>";
                            echo "</div>";
                            echo "</div>";
                        }
                    } else {
                        echo "<div class='col-12'><p class='text-center'>No products found.</p></div>"; //if there are no products in  a categpry or at all
                    }

                    $conn->close(); // Close the database connection
                ?>
            </div>
        </div>
    </section>

    <!-- Footer with copyright information -->
    <footer class="py-5 bg-pink">
        <div class="container"><p class="m-0 text-center text-white">Copyright &copy; GlamLife</p></div>
    </footer>

    <!-- Bootstrap and custom JavaScript files for functionality -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="js/scripts.js"></script>
</body>
</html>
